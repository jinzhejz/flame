[project]
name = "crawler"
version = "0.2.0"
description = "Crawler Application"
dependencies = [
  "requests",
  "markitdown",
  "beautifulsoup4",
  "flamepy",
]

[tool.uv.sources]
flamepy = { path = "/usr/local/flame/sdk/python" }