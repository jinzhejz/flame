[project]
name = "pi"
version = "0.2.0"
description = "Monte Carlo Estimation of π by flamepy.Runner"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flamepy",
    "numpy"
]



[tool.uv.sources]
flamepy = { path = "/usr/local/flame/sdk/python" }